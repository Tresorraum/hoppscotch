{
	admin off
	persist_config off
}

:80 :3170 {
	@mock {
		header_regexp host Host ^[^.]+\.mock\..*$
	}
	
	handle @mock {
		rewrite * /mock{uri}
		reverse_proxy 72.60.195.248:8080
	}
	
	handle {
		reverse_proxy 72.60.195.248:8080
	}
}
